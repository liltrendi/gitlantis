// import * as THREE from 'three';
import { type IUseMusicResponse } from '@/hooks/useScene/useMusic/types';
// import { useEffect } from 'react';

export const useMusic = (): IUseMusicResponse => {
	// const audioLoader = new THREE.AudioLoader();
	// const listener = new THREE.AudioListener();
	// const sound = new THREE.Audio(listener);

	// useEffect(() => {
	// 	audioLoader.load('/assets/music/ocean-waves.mp3', (buffer) => {
	// 		sound.setBuffer(buffer);
	// 		sound.setLoop(true);
	// 		sound.setVolume(0.5);
	// 		sound.play();
	// 	});
	// }, []);

	return {};
};
